# Handoff Doc — Around the Word (MVP Build)

*Tracks decisions made while implementing the MVP, and why. Companion to [app-design-doc.md](app-design-doc.md), which covers product/design decisions — this one covers what those decisions turned into in code, plus the smaller technical calls made along the way.*

---

## ⚠️ Product direction has pivoted three times since this code was built

Everything below this note describes the **English/TEFL-framed MVP** — subject list → phrase list, no exercises, no place/language concept. Since then the product direction has moved on three times:

1. First to a **trip-specific, place-first app** (Costa Rica/Spanish, activities → bilingual phrases + exercises + a grammar-tagged vocab system + a phrasebook) — captured in [app-design-doc.md](app-design-doc.md).
2. Then, per [language-app-system-design.md](language-app-system-design.md), **scoped down and restructured** to: a continent/country map for destination selection, checkbox category selection (back to the original generic subject list, not the Costa-Rica activity list), a cosmetic "personalizing your dictionary" step, and a **Learn/Use fork** — Learn is flip-card flashcards, Use is the original category→vocab-list flow with a flashcard option bolted on. No fill-in-blank exercises, no grammar tagging, no phrasebook, no accounts/persistence in V1. This is the version most of "Implementation Decisions" below describes, and it's itself now superseded.
3. **2026-08-06 — the current pivot, and now the authoritative direction:** the app inverts from "static JSON bundled in the Flutter app" to "a hand-curated Supabase/Postgres travel database, with the Flutter app as one read client onto it." Country pages replace the old flat vocab flow as the core screen — five tabs (Overview, Explore, Guide, Travel Info, Language), with the old checkbox-categories → personalizing → Learn/Use flow surviving intact as just the Language tab's sub-flow. Real schema now exists for weather, advisories, visas, points of interest, tips, leadership, and a friend-verification pipeline, alongside a much deeper language model (words/phrases/tokens, not just phrase/translation pairs). Captured in three new docs: [around-the-word-client-design.md](around-the-word-client-design.md), [around-the-word-data-architecture.md](around-the-word-data-architecture.md), [around-the-word-scratch-notes.md](around-the-word-scratch-notes.md) — see `CLAUDE.md`'s Current Plan section for the condensed version. Colleen's own framing: "override whatever I've told you so far with whatever is in these sheets even if it means rearchitecture."

**The vocab grammar-tagging pipeline (spaCy) work described below is still relevant groundwork** — even though V1's UI only reads `phrase`/`translation`-equivalent fields today, the new schema's `words`/`phrases` split (dictionary form vs. surface form, tokens, `is_maskable`) is a direct continuation of the same "capture grammatical structure now, cheap; backfill later, expensive" reasoning that motivated tagging `partOfSpeech`/`gender`/`number` in the first place. `tools/vocab_tagger/` (Python + spaCy, venv-isolated, not a Flutter runtime dependency) is still the right tool for generating that tagging, it just now feeds a richer schema than the flat per-country JSON files it originally targeted.

The codebase itself (the actual Flutter app, `lib/`) has **not yet been rebuilt** for pivot 3 as of this note — the mock-data work below is the first step of that, not a completed migration. Everything from "Current State" through the end of "Implementation Decisions" describes pivot 2's build, which is itself now historical, kept for reference (data-modeling reasoning, testing gotchas, etc. that may still transfer) rather than as a description of what's currently in `lib/`.

**2026-08-09 — the three pivot docs were refined, not re-pivoted.** A `why_short`/`why` split for best-times display, a full External Data Sources audit (condensed into `CLAUDE.md`), and the US State Dept Consular Affairs API resolving the advisory/visa-import open question. Nothing here changes the pivot-3 direction described above — see `CLAUDE.md`'s "External data sources" section for the condensed version, or the data architecture doc for the full one.

**Note on the previous paragraph's "not yet been rebuilt for pivot 3" line, below:** stale as of 2026-08-17 — `lib/` now has a real country-page build (`CountryHeader`, `SiteHeader`, the Overview-tab sections) underway against Costa Rica mock data. Left as-is rather than rewritten; this note exists so a future session doesn't take that line at face value.

### Gotcha: `flutter clean` needs a follow-up `flutter pub get`

Surfaced 2026-08-17 mid-way through the `CountryHeader`/ticket-page restyle: running `flutter clean` to rule out a stale-build explanation for a UI change not appearing deletes `.dart_tool/package_config.json` along with the build cache. Without that file, the IDE/analyzer can't resolve *any* import (package or relative) and throws errors across effectively every file in `lib/` and `test/` — reads exactly like widespread source breakage, but nothing in `lib/` actually changed. Fix is `flutter pub get` (regenerates `.dart_tool/`), then `flutter analyze` to confirm clean. Worth trying *before* recommending `flutter clean` as a troubleshooting step, not after.

### 2026-08-18: country page rebuilt as a collapsible-sections accordion, scoped to just that screen

Colleen sent `trip-dashboard-v5.html` — two frames (default-collapsed, all-expanded) of the country page redrawn as six independently-collapsible sections instead of the always-expanded flat stack the 2026-08-15 restyle built, with instructions to "override any existing design decisions with what you see here." Flagged the conflicts before building (per the Working Style note in `CLAUDE.md`) since this reverses two very recent, explicit calls:

- **The palette is cool-toned** (sky blue, lavender, sage) — exactly the family `design-preferences.md` rejected the day before ("stay in the warm/cream family... cool or blue-leaning tones don't fit this page"). Kept as a flagged, deliberate exception — see that doc's note on it, added the same day as this entry.
- **Visa & Entry and Travel Advisory split back into two independent sections**, reversing the 2026-08-11 "one shared source, not two" call. Each now cites its own source line even where (Costa Rica) it's really the same State-Dept page twice — simpler than connecting a shared footer across two cards a reader might collapse independently.
- Typography swapped Cormorant Garamond/Libre Baskerville/Public Sans/Courier Prime for **Fraunces/DM Sans/DM Mono** (fonts pulled from the `google/fonts` GitHub repo's `ofl/` source TTFs, bundled locally per the existing offline-first font policy — see `pubspec.yaml`'s `fonts:` section).

**Scoped to the country page only, confirmed with Colleen first** — `CountryTheme` already drives the app's global `MaterialApp` theme, `DestinationScreen`, and the shared `SiteHeader`, none of which this mockup shows. Rather than repoint `CountryTheme` itself, the new tokens live in a parallel `lib/theme/accordion_theme.dart` (`AccordionTheme`), consumed only by `CountryHeaderPreviewScreen` and the section widgets exclusive to it (confirmed via grep before editing: `CitiesSection`/`LanguagePairSection`/`BestTimesSection`/`PracticalNormsSection`/the old `TravelInfoSection`/`CountryHeader` have no other consumers). `SiteHeader` gained optional color-override params (default unchanged, so `DestinationScreen`'s instance is untouched) so this screen's instance can go ink-themed without a site-wide repoint. `CountryTheme` itself is untouched.

**New `AccordionSection` widget** (`lib/widgets/country_page/accordion_section.dart`) wraps every section: collapsed shows title + a one-line meta subheading derived from real bundle fields (never invented — e.g. Visa's meta is a truncated `VisaInfo.summary`, not a fabricated "visa required: yes/no"); expanded hides the subheading and shows the section's content, or a "Coming soon" placeholder when the underlying list/field is empty. **Every section always renders as a row regardless of data** — confirmed directly with Colleen, a deliberate difference from the rest of the app's "omit an empty tab" rule (client design doc), which is about whole tabs, not sub-sections within an already-shown page.

`TravelInfoSection` (the old combined widget) is deleted; `VisaSection` and `TravelAdvisorySection` replace it. `test/widgets/travel_info_section_test.dart` (covered the old desktop side-by-side/shared-source behavior, both gone) was replaced with `test/widgets/visa_and_advisory_sections_test.dart`. `test/widget_test.dart`'s smoke test updated to expand the Cities section before asserting a city name is present, since content only mounts once its section is open.

**Verification**: `flutter analyze`/`flutter test` both pass. Visually spot-checked via a temporary widget test that rendered `CountryHeaderPreviewScreen` to PNG (`RepaintBoundary.toImage`, with fonts explicitly loaded via `FontLoader` since `flutter_test` doesn't load custom fonts by default) rather than a live simulator — caught and fixed a real `RenderFlex` overflow in `CitiesSection`'s row (`city.name` needed `Flexible`+ellipsis, not an unconstrained `Row` inside `Expanded`) before it would have hit a device. The screenshot test file itself was temporary and deleted after use, not committed.

**Not addressed by this pass**: the five-tab IA (Overview/Explore/Guide/Travel Info/Language) the client design doc still documents as "resolved" is increasingly out of step with what `CountryHeaderPreviewScreen` actually is now (a flattened accordion spanning what were Overview/Travel-Info/Language content). Worth a real doc pass once Explore/Guide's remaining content (POI landmarks, cuisine, dress, festivals, history) actually needs a home — not blocking today's work since none of that is built yet either way.

### 2026-08-18 (later same day): two small accordion-restyle fixes, then `DestinationScreen` brought onto the same palette

Two quick corrections against the mockup, both screenshot-verified:
- **Word of the Day card**: the mockup's `.card-white` is a white card with a thin rose ring *inset a few px from the edge* (its CSS: `box-shadow: inset 0 0 0 3px #fff, inset 0 0 0 5px rgba(196,80,112,.35)`), not a top accent stripe — `_WordOfDayCard` (`language_pair_section.dart`) was using the latter. Recreated as two nested containers (Flutter has no direct inset-ring equivalent): an outer white fill for the 3px gap, an inner bordered box for the ring itself.
- **Visa & Entry's collapsed row** read as the same solid sky blue as the ticket stub directly above it. The mockup's `.sec-visa` is actually a distinct pale-blue-to-white diagonal gradient (`.sec-cities`/`.sec-times`/etc. gradient *to* their own pastel; `.sec-visa` uniquely gradients *to white* instead), not the flat `--sky` token. Added `AccordionSection`/`_Row` a new optional `gradient` param (`Material.color` → `Colors.transparent` + an `Ink`-decoration gradient when set) so only this one section overrides its flat `tint` — `AccordionTheme.visaRowGradient` is the new token, everything else unchanged.
- **"Expand all info ↓" / "Collapse all info ↑"** — was a mix of "info" on one and not the other, and a right-pointing arrow that didn't read as "reveals content below." Made both sides say "info" and pointed the arrow down.

**Then, per Colleen: "update the homepage with these fonts and a color that aligns with what we have laid out"** — `DestinationScreen` repointed from `CountryTheme` to `AccordionTheme`, matching the country-page accordion's palette/type (Fraunces heading via a new `AccordionTheme.pageHeading`, DM Sans/DM Mono for the search field and results, `AccordionTheme.page` background). `PaperTexture` dropped from this screen along with it — matches the country page's flat background, though `PaperTexture` itself is untouched and still used by `AboutScreen`. This is a second, explicitly-confirmed exception to "scoped to just the country page" (the original 2026-08-18 entry above) — not a silent reversal of that scoping call, Colleen asked for it directly this time.

**`SiteHeader` gained `homeLabel`/`homeLabelStyle`** — a wordmark ("Whereabout", per Colleen: "add whereabout next to the world icon for the home page") rendered next to the globe. `DestinationScreen` passes no `onHomeTap` (it's already home), so previously that side of the header rendered nothing at all (`SiteHeader`'s old logic: no `onHomeTap` → `SizedBox.shrink()`); now a label without a tap handler renders a **static** `Icon` instead of an `IconButton` — a logo mark, not a dead button.

**Correction, same day**: initially scoped `homeLabel` to `DestinationScreen` only, reasoning "the country page's `SiteHeader` instance is unaffected... confirmed with Colleen that `SiteHeader` should stay shared, just with this one screen opting into the label." That reasoning was wrong — Colleen's actual ask was that both screens show the *identical* header, wordmark included ("the header on the home page should be the same header on the destination page... right now it looks like the destination page has a different header with no Whereabout"). `CountryHeaderPreviewScreen`'s `SiteHeader` call now passes the same `homeLabel: 'Whereabout'` + `homeLabelStyle` as `DestinationScreen`'s — both call sites are visually identical now (still two call sites, not a shared constant, since `CountryHeaderPreviewScreen`'s also passes a real `onHomeTap`).

**Three more small fixes, same pass**:
- Search field's focus-ring color (`skyDark`, a blue) read as off-theme — nothing else on the home page is blue (`skyDark` is reserved for the country page's Visa & Entry section specifically). Switched to `AccordionTheme.ink`, matching the header/heading/chevron color already used everywhere else on the page.
- Found and fixed a related bug while checking that: the search `TextField` didn't set its own `filled`/`fillColor`, so it was inheriting `main.dart`'s app-wide `InputDecorationTheme` — `CountryTheme.card`, a cream/tan left over from the old navy/gold theme, visibly clashing with the new palette. Same root cause as the border color complaint (something not on `AccordionTheme` bleeding through), just not the specific thing Colleen pointed at — worth mentioning since "one thing looks off-theme" is a good signal to check the same widget for *other* un-migrated defaults, not just the one flagged. Set explicitly to `AccordionTheme.white`.
- `SiteHeader`'s container padding (16/10) tightened up against its own text once the wordmark was added — bumped to 20/16 ("add some more padding around any of the text in the header").
- `debugShowCheckedModeBanner: false` added to `main.dart`'s `MaterialApp` — the diagonal debug ribbon was showing app-wide in every debug build, not just this screen's screenshots.

### 2026-08-18 (still later): app-wide theme repoint, and `ComingSoonScreen` retired from the destination flow

Two more, per Colleen: "let's align any remaining pages with the new stylings" + "instead of the coming soon page we should just have a country page showing any available data where the expanded categories say 'coming soon.'"

**Theme repoint**: `main.dart`'s app-wide `ThemeData` (`_theme`) switched from `CountryTheme` to `AccordionTheme`. Checked via grep first: `CategorySelectionScreen`, `CategoryListScreen`, `PersonalizingScreen`, `LearnUseScreen`, `VocabListScreen`, `FlashcardScreen` have zero direct theme-file references — they exist purely on whatever `ThemeData` gives a plain `Scaffold`/`AppBar`/`CheckboxListTile`/etc., so this one repoint carries all of them onto the new palette/type for free, same "deliberately shallow" strategy the file's doc comment already described for the old theme. `AboutScreen` (the one screen with a direct `CountryTheme` reference, plus its own `PaperTexture`) got a small hand-edit to match — dropped `PaperTexture` for the same flat-page-background reason `DestinationScreen`/`CountryHeaderPreviewScreen` already did.

**`ComingSoonScreen` retired from `DestinationScreen._selectCountry`**: every tapped country now pushes `CountryHeaderPreviewScreen` directly, `active` or not — the `if (!country.active)` branch that used to detour to `ComingSoonScreen` is gone. This is what actually makes the empty-shell behavior from the *first* 2026-08-18 entry above (`_emptyBundle`, "coming soon" per section) reachable for real: it existed in the code already (for the "bundle file missing" catch case) but was previously unreachable in practice since `active: false` countries never got that far. `Country.active` itself is untouched on the model — just no longer read anywhere — kept rather than removed since it's still meaningful data (whether a country is actually being actively curated), just not a navigation gate anymore.

`ComingSoonScreen`, its `coming_soon_resources`/`TripSelection.resources` plumbing, and `test/no_content_country_test.dart`'s old coming-soon assertions are all superseded by this — the screen file itself is left in place (same "kept as history, not deleted" pattern as elsewhere in this doc), but `no_content_country_test.dart` was rewritten in place (same filename, new assertions: selecting an uncovered country opens the country page, every section present as a row and collapsed by default, expanding one shows the "Coming soon" body) rather than left testing retired behavior.

Also updated `around-the-word-client-design.md`'s Screen Flows/MVP Scope/Roadmap/Open Questions to match — the "Coming-soon screen" it documented as a separate dead-end is now folded into the country page; flagged a genuinely open question there (do the old `coming_soon_resources` links resurface inside an empty section, or just drop) rather than deciding it unilaterally.

### 2026-08-18 (still later): every section-row gradient, not just Visa's — and a white seam between header and body

Per Colleen: the section headers "almost bleed in" to their own body content, and the gray divider under them "not doing what we'd like."

Root cause, once actually compared against the mockup again: `trip-dashboard-v5.html`'s `.sec-*` classes are *all* diagonal gradients (a low-alpha dark accent fading to the section's pastel by 55%) — the first accordion pass only ported that for `.sec-visa` (because *that* one specifically read as the same flat blue as the ticket stub above it) and flattened the other five to a plain solid pastel, as a shortcut. That flattening is exactly why Cities/When to Visit/Practical Norms "bled in" — their header row and their body content share the same solid pastel with zero visual texture to separate them, whereas Visa/Advisory/Language have enough of a color jump to their (partly) white body that the flat-vs-gradient gap wasn't as visible. Not a new design decision, just finishing one that was only half-applied — see `AccordionTheme`'s new `citiesRowGradient`/`timesRowGradient`/`advisoryRowGradient`/`languageRowGradient`/`normsRowGradient`, hand-blended against `page` the same way `visaRowGradient` originally was (predictable flat colors, not literal alpha compositing).

**Also added**: a deliberate white seam (`AccordionSection`, a 5px white bar with a soft drop shadow) between the header row and its expanded content, replacing reliance on the header row's own 1px `ink@8%` hairline for that specific job — that hairline still marks collapsed-row-to-collapsed-row boundaries fine, but was too low-contrast to read as a real separator once expanded, especially against a same-hue body. Screenshot-verified against Costa Rica's full bundle, all sections expanded.

### 2026-08-18 (last one): `SiteHeader` stopped taking its look as constructor params

Colleen noticed the icon-to-"Whereabout" gap looked slightly different between the home and country pages. Root cause: `onHomeTap != null` rendered an `IconButton`, `onHomeTap == null` rendered a bare `Icon` — even with the `IconButton`'s padding/constraints zeroed out, it wasn't landing pixel-identical to the plain `Icon`'s box. First fix: swap `IconButton` for the same `Icon` wrapped in `InkWell` when tappable, so both call sites render the literal same widget tree for the icon.

That fix prompted the real question: **why did `backgroundColor`/`iconColor`/`aboutTextStyle`/`homeLabel`/`homeLabelStyle` exist as constructor params at all**, when both of `SiteHeader`'s only two consumers (`DestinationScreen`, `CountryHeaderPreviewScreen`) passed the exact same values for every one of them? Per Colleen: "we shouldn't be having separate code for this on different pages regardless." That duplication — two copies of the same five-param block, one per screen — is exactly what let the `IconButton`/`Icon` split happen unnoticed in the first place; nothing forced the two call sites to stay in sync.

Collapsed `SiteHeader` back down to just `onHomeTap`/`onAboutTap` (the two things that legitimately differ by screen — home has no home button, country page does). Colors/fonts/the "Whereabout" wordmark are now hardcoded once inside the widget, not passed in. Both call sites shrank from ~15 lines of repeated styling to two lines each. If a screen ever genuinely needs `SiteHeader` to look different, that should be a deliberate new option (e.g. a `variant` enum) added for a real, stated reason — not a per-instance override left open by default the way these params were.

### 2026-08-18 (one more): the white header/body seam reverted, replaced with a perforated "tear line"

Per Colleen: revert the plain white 5px bar from earlier, but the header-bleeding-into-body problem it was solving is still real — find something more "creative and cool" that still fits the page.

Landed on reusing this app's own established ticket-stub motif rather than inventing something new: `TicketPanel`'s dashed/perforated edges and `DashedDivider` (already used in the ticket header between the name block and the time/currency stub) are exactly this app's existing "tear line" language. `AccordionSection` now renders a short band still in the header's own `tint` color, punched through by a `DashedDivider` in the section's own "dark" accent (`accentDark` — new required param, `AccordionTheme.skyDark`/`lavenderDark`/`butterDark`/`sageDark`/`roseDark`, plus a new `peachDark` added for Practical Norms since the mockup never named one for `--peach`) — reads as "where the ticket stub tears off" before the white content below, colored per-section rather than one flat white bar everywhere.

**Real bug caught while verifying this, not just a style note**: the first attempt (`Container(height: 12, color: tint, alignment: Alignment.center, child: DashedDivider(...))`) rendered *nothing* — the dashes were invisible, not just subtle. Root cause: `Container.alignment` loosens the incoming constraints for its child, and `DashedDivider`'s `SizedBox` only fixes *height* (width is left unconstrained, matching every existing call site, which all sit somewhere already providing a tight/bounded width — a `Row`'s `Expanded`, a `Column`). Under loosened constraints there's nothing forcing a nonzero width, so it collapsed to zero and painted nothing. Fixed with `Center(child: SizedBox(width: double.infinity, child: DashedDivider(...)))` — `double.infinity` resolves against the loosened constraint's own max, forcing full width back. Caught by literally reading pixel values out of a rendered screenshot with Pillow rather than eyeballing a scaled-down phone-frame image — a solid-color band and an invisible-dashes band look identical at a glance when the container and the dash color are close in value, and just as identical when the dashes are literally width-zero. Worth doing that pixel-level check again for any future divider/line-on-colored-background work, since "the screenshot looks plausible" isn't the same as "the thing I added is actually painting."

**Follow-up, same day**: once the dashes actually rendered, Colleen flagged a second, real issue — `_Row`'s own bottom border (the `ink@8%` hairline that's always been there, separating one collapsed row from the next) was still rendering on an *expanded* row too, stacking directly above the new perforated tear-line and reading as two redundant dividers ("very funky looking"). That hairline is now collapsed-state-only (`decoration: expanded ? null : BoxDecoration(...)`) — an expanded row's only separator is the tear-line below it now, one clear seam instead of two.

**Third follow-up, same day**: still "weird" against the *gradient* headers specifically (Colleen: "the perforation needs to be right where the border between the heading and the body is, no padding above it"). The tear-line band was a fixed 12px tall with the dash `Center`-aligned inside it — a few px of flat-[tint] fill sat above the dash, between it and the header's actual bottom edge. Against a flat-tint header that gap was invisible (same color both sides); against a *gradient* header, the gradient's own color right at that edge is diagonal, not uniform, so the flat-tint gap didn't quite match it — reading as an extra, slightly-off transition of its own. Fixed by dropping the fixed-height band and the centering entirely: the colored strip is now exactly as tall as the dash's own stroke (`DashedDivider(height: 3, ...)`), sitting immediately at the header/body seam with nothing above it.

**Fourth follow-up, same day**: made fainter still — `accentDark.withValues(alpha: 0.35)`, `strokeWidth` down to `1.25`. Full-strength color read as too strong an accent for what's meant to be a quiet texture cue, not a decoration in its own right.

### 2026-08-18 (the big one): per-country section colors, derived from each flag's actual colors

Colleen asked directly (a second time — "I might've already tried to ask you to do that") how hard it'd be to derive the country page's color theme from each country's flag. Worth the context: something adjacent already existed and was turned off on purpose — `CountryFacts.accentColorHex` + `CountryTheme.lightTint` + `CountryHeaderPreviewScreen._useAccentPageBackground` (2026-08-11, then disabled the same day: "I want the page theme to be more overarchingly cohesive... a different pastel per country was working against the one identity"). Flagged that explicitly before building anything, per the design-preferences.md gate-check process established earlier this same session — she confirmed she wants the full version anyway, reopening that call on purpose this time.

**Mechanism**: `package:palette_generator` (official Flutter-team package, `flutter/packages` monorepo — currently marked discontinued on pub.dev but still functional and with no listed replacement; worth revisiting if that changes) extracts real color swatches from each country's flag PNG (`flagcdn.com`, already fetched for display). `lib/utils/flag_palette.dart`'s `extractFlagHues` pulls `vibrant`/`lightVibrant`/`darkVibrant`/`muted`/`lightMuted`/`darkMuted` swatch hues (falling back to `dominantColor` if none of those resolve) — `lib/theme/section_palette.dart`'s `SectionPalette.fromFlagHues` then cycles those real hues across the six accordion sections (Visa & Entry, Cities, When to Visit, Travel Advisory, Language, Practical Norms — repeating when a flag has fewer than six distinct hues, which is most flags), each rendered at fixed pastel/dark lightness bands so every section still reads as a proper card regardless of how saturated the source swatch was. Only the *hue* comes from the flag; the light/dark shaping is this app's own consistent rule.

**First version was wrong, corrected same day**: the initial `SectionPalette.fromSeedHue` took *one* seed hue and spread it 60° apart six times — a generated rainbow, not the flag's own colors. Colleen caught this immediately from the rendered result: "I want the colors of the sections to only be variations of the colors in that country's flag." Rebuilt around `PaletteGenerator`'s actual swatch targets instead of one hue + a rotation rule — see `extractFlagHues`'s doc comment for the full reasoning on why that's the right primitive (a two-color flag like Japan's should genuinely only yield one or two hues back, repeated, not six invented ones).

**Threading**: `AccordionSection`'s `tint`/`accentDark`/`gradient` params (already instance-level, not `AccordionTheme` statics, from the earlier gradient-texture work) meant the accordion rows themselves needed no changes — only `CountryHeaderPreviewScreen`'s six `_xSection()` builders had to switch from `AccordionTheme.sky` etc. to `_palette.visa.tint` etc. Every section's *content* widget (`CitiesSection`, `BestTimesSection`, `PracticalNormsSection`, `LanguagePairSection`, `VisaSection`, `TravelAdvisorySection`) and the ticket stub (`CountryHeader`) needed a new `tint`/`accentDark`/`accent` param each, replacing what were previously hardcoded `AccordionTheme.<color>` references — a few of which turned out to be leftover cross-section mismatches once actually parameterized (Travel Advisory's "Full advisory" link was hardcoded `skyDark`; the Word of the Day card's pronunciation text was hardcoded `lavenderDark` regardless of Language's own color) — both now correctly use their own section's accent.

**Network-dependent, best-effort, never blocking**: `_loadPalette()` runs independently of (in parallel with, not after) the bundle load — `_palette` starts at `SectionPalette.fallback` (the original hand-picked Costa-Rica-era values, byte-for-byte) and only updates via `setState` if extraction actually succeeds; any failure (offline, decode error, no usable swatch) just leaves the fallback in place silently. The page never blocks on or breaks over this.

**Verification note**: confirmed the extraction *logic* is correct in isolation (`extractFlagHues('JP')` → hues around 347°, the red/pink family, in ~120ms — matches Japan's flag). Getting a full widget-level screenshot proof through `flutter_test` turned into a losing fight against the harness (it mocks all HTTP to return 400 by default; working around that with `HttpOverrides.global = null` re-enables real sockets, but mixing real network I/O with `flutter_test`'s FakeAsync zone is unreliable — real TLS handshakes sometimes never resolve within the test's pumped timeline). Separately, `PaletteGenerator.fromImageProvider` itself doesn't attach its own `ImageStream` `onError`, which made failed fetches get reported to `FlutterError.reportError` regardless of a surrounding `try`/`catch` — worked around in the real implementation by resolving the `NetworkImage` by hand with a real `onError` listener, then calling `PaletteGenerator.fromImage` on the decoded image instead of `fromImageProvider`. Per Colleen — she tests visual/network-dependent changes herself by running the app, not via generated screenshots — stopped chasing full automated proof once the underlying logic was confirmed correct; she'll check the live rendering herself.

### 2026-08-18 (the big one, corrected twice more): base-only colors, no lightening, computed text contrast

Colleen looked at the actual rendered result (Costa Rica) and corrected two things in the same message, the second one mid-sentence:

1. **"I only want to incorporate base flag colors, not every color in the seal on the flag as well"** — pointed at purple/green/orange showing up in the Costa Rica render, none of which are in the flag's blue/white/red field, all of which are in its small central coat-of-arms seal. Root cause: extraction was using `PaletteGenerator`'s `vibrant`/`muted`/etc. *target* swatches, which are deliberately tuned to surface visually distinctive colors regardless of how much of the image they actually cover — exactly backwards for "the flag's real colors." Rewritten around `paletteColors` sorted by **`population`** (the pixel count each quantized bucket represents) instead, keeping only buckets at least 8% of the single largest one — a flag's field stripes are enormous relative to a small emblem, so this separates them cleanly without needing to know anything about where the emblem actually sits. See `extractFlagBaseColors`'s doc comment.

2. **"by pastel I simply mean a closer to white version... not a different position on the color gradient"** — then, mid-message: **"actually, you know what, don't even do a pastel version. Just do the main colors that are in the flag."** `SectionColors.tint` is now the extracted color exactly as found — no lightness/saturation adjustment, no gradient, nothing derived. `SectionPalette`'s `_colorsFor` helper (and the whole gradient-generation machinery, `visaRowGradient` and its five siblings) went away entirely — see `AccordionTheme`'s class doc for what got removed and why the few colors that survived (`skyDark`/`butterDark`/`roseDark`) did.

3. **Text/UI-element contrast, not a hue-name list**: Colleen's own fallback suggestion was "if the color is white yellow or orange... otherwise black," but she explicitly invited a real function instead ("if you can create a better function for deciding which color the font should be based on the hex code please do"). Added `lib/utils/contrast_color.dart`: `readableTextColor` picks plain black or white via the actual WCAG 2.0 relative-luminance/contrast-ratio formula (correct for *any* color a flag could contain, not just the three she named), and `accentReadableOnWhite` darkens a color by lightness only (same hue/saturation — the one place that "move along the lightness axis, don't shift hue" idea from her first message actually landed) just far enough to stay legible when used as a link/border/badge accent *inside a white content card*, where the raw flag color might itself be too light (a white or pale-yellow field, say) to read there.

**Fallout — full-bleed content now needs contrast-aware text everywhere, not just section headers**: since `tint` is no longer a guaranteed-light pastel, every place that uses a section's `tint` as a background for real content (not just the collapsed header row) needed its hardcoded ink-colored text swapped for the computed `textColor` — `CitiesSection`'s whole city list, `BestTimesSection`'s month/reason text, `PracticalNormsSection`'s icon+title+detail rows, `LanguagePairSection`'s languages card. `SectionColors` ended up with three fields instead of the original two: `tint` (the raw color), `textColor` (black/white for text sitting on `tint`), `accentOnWhite` (a legible-on-white variant for accents inside white cards — `VisaSection`/`TravelAdvisorySection`/the Word of the Day card, none of which have a `tint` background at all). `AccordionSection`/`_Row`'s own hardcoded `ink`-based chevron and hairline-divider colors got the same treatment, both now keyed off `textColor`.

### 2026-08-18 (one more): subheadings hidden, and a new "Additional Resources" section

Two more from the same round of feedback on the rendered result:

**Collapsed-row subheadings off for now** — Colleen: "maybe we remove the subheaders or at least hide them for now." Kept as a named flag (`AccordionSection.showSubheading`, `false`) rather than deleting the underlying `meta` plumbing — every `_xSection()` builder in `CountryHeaderPreviewScreen` still computes real `meta` text from bundle data, `_Row` just doesn't render it while the flag is off. Same "kept, not deleted" pattern `_useAccentPageBackground` already established for this codebase. Two tests (`widget_test.dart`, `no_content_country_test.dart`) had assertions on that collapsed-state text ("4 destinations", "Coming soon" as a meta line) that needed dropping — the *expanded* "Coming soon — we haven't curated this yet." body text is unrelated (a different code path, `_ComingSoon`) and still asserted.

**New "Additional Resources" section** — the seventh and last accordion section, per Colleen. This is also the resolution to an Open Question the client design doc had flagged since the coming-soon-screen retirement: what happens to the generic resource links (`Resource`/`TripSelection.resources`, from `assets/data/resources.json` — "Google Translate," "Wikitravel") now that `ComingSoonScreen` doesn't read them anymore? Rather than nesting them inside another section's empty state (the option that doc's Open Questions entry had floated), they get their own section — `lib/widgets/country_page/additional_resources_section.dart` — shown on every country's page, not just uncovered ones, since a link like Google Translate is useful regardless of how complete a country's own content is. `SectionPalette` grew a seventh `SectionColors` slot (`resources`) generated the same way as the other six (cycling the same extracted flag colors, no special-cased neutral tone) so the new section doesn't need a different color story. `CountryHeaderPreviewScreen` now reads `TripSelection.resources` via `context.watch` (already loaded app-wide, previously only consumed by the now-mostly-dead `ComingSoonScreen`).

### 2026-08-18 (final round): bigger text, perforation gone for good, a deepened per-country masthead, toned-down saturation, and paper texture back

Five fixes from one message, after Colleen looked at the rendered per-country palette: "the black header/country destination part now looks weird... let's make all the text at least a bit bigger. let's also remove the perforated lines... what should we do about the top black part... I feel like the colors are a bit too bright as well. what other design conventions... could help us make this look more full and less AI/like anyone could make it."

**Text sized up across the board** — essentially every `AccordionTheme` text style got a font-size bump (`tkEyebrow` 9→10, `tkNative` 11→12.5, `tfVal` 24→27, `secName` 17→19, `sHead` 16→18, `rowTitle` 14→15.5, and others in between), plus `DestinationScreen`'s heading (28→31), `CountryHeader`'s name (`isDesktop ? 48 : 34` → `50/36`), and `SiteHeader`'s wordmark/about-link. Same "size up rather than down" instinct design-preferences.md already documents from the 2026-08-17 ticket-stub sizing pass — extended here to the whole type scale, not just one card's fields.

**Perforated tear-line removed, for good this time** — the dashed `ColoredBox`/`DashedDivider` pairing between an expanded row and its body (added, then corrected twice more, earlier the same day) is deleted outright per "let's also remove the perforated lines," not just tweaked again. `AccordionSection` now has no divider element at all between header and body — the `tint`→white color change at that boundary is the only seam, plain per Colleen's direction rather than another texture attempt.

**The "weird black header" question, answered with a third option, not her tentative one** — Colleen's own message floated flipping it to white-bg/black-text but asked directly "what should we do... from a design best practices perspective." Diagnosed the actual problem as identity mismatch, not color-scheme direction: a fixed generic-black masthead sitting above a page that's now vividly *this specific country's* colors read as leftover chrome from before the per-country system existed, not a deliberate light/dark choice either way. Fix: `SectionPalette` grew an eighth token, `header` — the country's own primary flag hue pushed dark (`_deepen`: same hue, saturation fixed at 0.42, lightness fixed at 0.15, so every country's masthead reads as "the same *kind* of deep, confident color" rather than however saturated that particular hue happened to start) rather than a flat literal black. `CountryHeader` takes this as a new, separate `header` field distinct from its existing `accent` (the vivid, un-deepened color the lighter ticket stub below still uses) — two intentional depths of the same hue, not one flat block. `SiteHeader` (the site nav bar directly above the masthead) gained one new optional `Color? backgroundColor` param so it can match — deliberately *one* input, with text/icon color auto-derived via `readableTextColor` rather than the multi-param styling duplication that caused the icon/wordmark gap bug earlier in the session; see that class's doc comment for the full "why this reopens `SiteHeader`'s one-fixed-look rule, on purpose, with a stated reason" reasoning. `CountryHeaderPreviewScreen` passes `backgroundColor: _palette.header.tint` / `header: _palette.header` at the two call sites.

**"Colors are a bit too bright" — a saturation fix, not a walk-back of "no pastel."** Added `_toned()`: same hue, same lightness, saturation × 0.72. This is deliberately a different axis from the lightening/pastel approach Colleen rejected twice earlier the same day ("don't even do a pastel version") — full write-up of the distinction is in `SectionPalette`'s class doc. Applied to every section's `tint`; `accentOnWhite` (small links/badges/borders) still derives from the un-toned raw color, since a small accent reads fine at full intensity in a way a large fill doesn't.

**Paper texture reintroduced on the country page**, as one concrete answer to "what else could help this look less AI/like anyone could make it" — `PaperTexture` already existed in the codebase for exactly that stated reason (added 2026-08-11, "a single flat page color with slightly-lighter flat cards on top is the exact 'AI-default' look") but had been dropped from every screen and had zero call sites left. Gave it an optional `color` param (defaulting to its original `CountryTheme.ink`) instead of hardcoding a second theme reference, and wrapped `CountryHeaderPreviewScreen`'s `Scaffold.body` in it with `color: AccordionTheme.ink`. At the texture's alpha (0.035/0.02) the exact hue barely reads — this is about grain, not adding another color — but matching the caller's own ink is still more correct than leaving it pointed at an unrelated theme.

Other ideas surfaced for this same "less AI-generic" question but not built yet, worth a look if the texture alone doesn't fully land: leaning further into the existing ticket/stamp motif elsewhere on the page now that the accordion's own perforation is gone (the rotated "stamp" corner mark on Visa/Advisory content is the one other place this codebase already does that); one consistent "signature" accent thread reserved for interactive elements specifically (a single recognizable color/shape used *only* for tappable things, distinct from the per-country tints); and deliberate small asymmetries (an icon or accent nudged off a perfectly centered grid) — all flagged as options to react to, not committed to, since none of them were asked for directly the way the five items above were.

### 2026-08-18 (more still): expand-all pill → flush strip, ticket stub gets its own color, and the site nav's "Whereabout" wordmark becomes the nav

A handful more rounds of feedback on the still-just-shipped final-round changes above:

**"Expand all info" background made white** (per Colleen), the first fix in a short chain — it used to just inherit whatever the ticket stub's background was.

**Chevron's expanded-state solid ink fill removed** — per Colleen: "we already have enough to designate that they're expanded (seeing the full section, the direction of the chevron)." `_Chevron` now renders the same outline-only circle whether expanded or not; only the arrow direction changes.

**The ticket stub (local time/currency) got its own color, `SectionPalette.stub`** — per Colleen: it was sharing `accent`'s exact tint with the Visa & Entry row directly below it, so the seam between "chrome" and "first section" disappeared. `stub` is a fixed-formula midtone of the country's primary hue (`_midtone`: saturation 0.38, lightness 0.30) — landing between `header` (darkest, 0.15) and a section's own lighter `_toned` tint, so the top of the page now reads as a deliberate three-step gradient (masthead → stub → first section) instead of two of those three steps matching.

**Then, once the stub had its own background: "Expand all info" became a floating white pill on top of it** — per Colleen's own direct ask. Followed immediately by a clarity concern from her: a floating pill visually read as *belonging to* the stub it sat inside, when what it actually controls is every section below it, not the stub itself. She proposed two fixes (right-align it under the chevrons it controls; or make it flush/square and touching the section below, not floating). Both were implemented together rather than picking one: the strip is now flush (no rounded corners, no bottom padding/border — the color change into the Visa & Entry row below is the seam, same "no divider" language every accordion row already uses) **and** right-aligned with its own chevron-style arrow icon, landing in the same horizontal spot as every section row's chevron below it — one repeating visual column of controls instead of two unrelated elements.

**Site nav top padding bumped again** (16→24 above the safe-area inset), per Colleen: "add more top padding for the header" — confirmed via `AskUserQuestion` that this meant `SiteHeader` (the nav bar), not `CountryHeader`'s masthead block, since "the header" is ambiguous between the two in this codebase (see design-preferences.md's note on always disambiguating that specific pair of terms).

**Flag size bumped** (52×36/38×26 → 60×41/44×30 desktop/mobile) — the masthead's reserved right-hand padding widened alongside it (78/62 → 86/68) to keep the same margin, not let the bigger flag creep toward the name text.

**`SiteHeader`'s wordmark itself became the nav, and the separate right-side "ABOUT" link was removed** — per Colleen: globe + "Where" now opens `onHomeTap`, "about" (the second half of the same word) opens `onAboutTap`; the standalone "ABOUT" text link that used to sit on the right of the header is gone as a now-duplicate destination. Colleen's first color idea was literal white/black for the two halves, but she caught the problem herself before it was built: `SiteHeader`'s `backgroundColor` is always dark in practice (the fixed `AccordionTheme.ink` default, or a `SectionPalette.header` deepened color — never anything light), so literal black would disappear into it rather than read as a second tone. Landed on `AccordionTheme.page` (the light lavender-grey that's the country-search page's own background) for "about" instead of black — same two-tone effect, stays visible against either background `SiteHeader` actually uses. `readableTextColor` is no longer used in this file at all now that both wordmark colors are fixed rather than derived.

### 2026-08-19: site nav layout bug, flag bigger + vertically centered + perforated "stamp" edge

**Bug: the site nav's background stopped filling the full header width.** Root cause was the previous entry's own wordmark-as-nav change — the outer `Row` was set to `mainAxisSize: MainAxisSize.min` (nothing left on the right anymore, so `mainAxisAlignment.spaceBetween` was dropped along with it), which shrank the `Row` to just its content's width — and since nothing else in `SiteHeader` declares an explicit width, the parent `Container` (and its background color) shrank down with it. Fixed by dropping back to `Row`'s default `mainAxisSize.max` — content stays flush left (default `mainAxisAlignment.start`), only the background now reaches full width again.

**Flag bumped bigger a third time** (60×41/44×30 → 68×46/50×34) and **vertically centered** — it was pinned to the masthead's top edge (`Positioned(top: 0, right: 0)` with no `bottom`, so `Center` had no room to actually center within). Now `Positioned` stretches `top: 0, bottom: 0` across the full masthead height, with a `Center` inside that having real room to work with.

**The flag's border became an actual perforated "stamp" edge, after a wrong first guess.** First pass (same round as the size bump) read "white stamp-like border" as a plain white photo-mat frame + drop shadow — reasonable, but not what Colleen meant. Corrected same day: "more like one of those borders with the tiny triangles like how stamps get perforated." New `_StampEdgePainter` (`CustomPainter`) traces the mat rectangle's perimeter with a zigzag of small outward-pointing triangles instead of a straight edge — walks each of the 4 sides in `toothSize` steps, alternating each step's endpoint between the straight baseline and a point pushed outward by `toothSize`, which is what forms the triangle teeth. The flag image itself is unaffected — it's a plain rect, still sized/positioned to sit centered inside the mat's straight-edged inset, so the zigzag only ever reads as a decorative perforated margin around it, never clips into the image itself.

**Per Colleen, 2026-08-19: `flutter test` isn't run for pure style/visual changes anymore** — "You don't need to run the test suites just for style changes." `flutter analyze` alone (compile/lint, not a real test run) is enough for anything that's only colors/spacing/sizing/decoration with no logic behind it; `flutter test` is reserved for changes that touch actual behavior/state/data flow. (Recorded in memory, not just here, since it governs every future session too.)

### 2026-08-19 (later): a placeholder footer, and a new "Neighbors" section

**`SiteFooter`** — a small shared copyright line at the bottom of every screen's content, per Colleen: "I want to say like copyright 2026 whereabout like a lot of footers but I can't say that yet." Reads `© 2026 Whereabout — placeholder` rather than the real line outright — obvious in the running app that it isn't final legal text yet, rather than silently shipping unverified copy. New file, `lib/widgets/country_page/site_footer.dart`, one shared widget (not copy-pasted per screen, same reasoning `SiteHeader` already established) — added to the bottom of `DestinationScreen`'s, `CountryHeaderPreviewScreen`'s, and `AboutScreen`'s content. Swapping in the real line later is a one-file edit.

**New "Neighbors" section** — per Colleen: "I would like a 'neighbors' section that lists all bordering countries of that country," reflected in model, mock data, UI, and design docs as asked.

- **Model**: `CountryFacts` gained `borderingCountryCodes` (`List<String>`, alpha-2, e.g. `["PA", "NI"]`) — not nullable, defaults to `[]`, same "empty is a real answer, not a missing one" reasoning `officialLanguages` already established (an island nation genuinely borders nothing). **Commodity, not curated** — REST Countries' `borders` field already covers this (CCA3 codes, needs a CCA3→alpha-2 conversion step at import time, not a field-to-field copy); hand-set in mock bundles only until the importer exists, flagged in the data architecture doc's External Data Sources table and `country_facts` schema alongside the other pre-importer commodity fields.
- **Mock data**: Costa Rica's bundle (`assets/data/bundles/cr.json`) got its two real land borders, Panama and Nicaragua (`["PA", "NI"]`) — both already real, selectable entries in `countries.json` (`PA`/`NI`), so they resolve for real rather than needing placeholder country data.
- **UI**: new `NeighborsSection` widget (`lib/widgets/country_page/neighbors_section.dart`), modeled on `CitiesSection`'s row style (full-bleed section-color fill) minus the numbering/featured-star, which are Cities-specific concepts. Each row resolves its code against the already-loaded `TripSelection.countries` list (passed in, not watched by this widget itself — kept a plain data-in/callback-out widget like every other section) to show a real country name and, if resolved, a tappable chevron; an unresolved code still renders as its raw ISO code rather than silently vanishing. Tapping a resolved neighbor calls `trip.selectCountry(country)` then pushes a new `CountryHeaderPreviewScreen` for it — the same select-then-push pattern `DestinationScreen._selectCountry` already uses, so jumping to a neighbor behaves identically to picking it from search. This is V1's first working example of one country page linking onward to another (Cities' city-page links are still just a visual affordance, not wired to anything yet).
- **Placement**: inserted right after Cities in the accordion (`visa → cities → neighbors → times → advisory → language → norms → resources`) — both are geography/place sections, and Neighbors' whole point is linking onward to more country pages, so it reads better next to Cities than tacked on wherever was structurally easiest. `SectionPalette` grew an eighth `SectionColors` slot (`neighbors`), cycling the same extracted flag colors as every other section, same reasoning `resources` already established for a section added after the original six.
- **Docs**: data architecture doc's External Data Sources table, `country_facts` schema block, `CountryFacts` Dart shape reference, and MVP Scope's commodity-facts bullet all updated; client design doc's Overview-tab bullet got a dated note alongside its existing Cities description.
- **Tests**: new `test/widgets/neighbors_section_test.dart` (resolved rendering, unresolved-code fallback, tap-callback wiring, empty-list no-op) — `flutter analyze` clean, all 42 tests (38 existing + 4 new) pass.

### 2026-08-19 (last one): comment style — short in code, detail in docs

Per Colleen, looking at `country_header_preview_screen.dart`: "I'm seeing a lot of really long comments in the code. I would prefer if we do not put comments more than a few words long for things in the code and leave any detailed explanations to the docs instead." Recorded in memory (governs future sessions too), then swept the whole `lib/` tree on request ("yes please sweep the others, not just from this session") — 41 files, no logic changes, only comment trims. The paragraph-length doc comments this session had built up (verbatim Colleen quotes, dated correction histories, full reasoning chains) are gone from the source; that history still lives in this file's dated entries above, which is where it belongs going forward. `flutter analyze` clean, all 42 tests pass throughout.

## Current State (of the pivot-2 build — historical, not current; see the pivot note above)

The four-screen flow from `language-app-system-design.md` is built and wired end to end, backed by placeholder content: Destination search → (Category checkboxes, or Coming-soon if inactive) → cosmetic "Personalizing your dictionary" step → Learn/Use fork. Learn opens a shuffled flashcard deck of everything selected; Use goes to a category list → (sub-subjects, if any) → vocab list, with a per-category flashcard option. One country has real content (Costa Rica/`cr`) out of all 219 in `countries.json` — every country is selectable from search, but only `active` ones reach the real flow; the rest dead-end at `ComingSoonScreen` (see Implementation Decisions). Content is placeholder — a handful of tagged entries under `food-cooking`, `museums`, and `hiking` — not real Costa Rica vocab yet. `flutter analyze` and `flutter test` both pass.

(This used to be a five-screen flow with a continent list → country list step; that was dropped 2026-07-30 in favor of a single search screen — see the Implementation Decisions entry below.)

## Implementation Decisions

- **State management: `provider`, not `riverpod`.** The doc left this open ("Provider, or Riverpod if a bit more structure is wanted"). Went with `provider` since the actual state shape is one flat `ChangeNotifier` (`TripSelection`) read by every screen — Riverpod's extra structure (providers-of-providers, code-gen) isn't earning its keep at this size. Revisit if state gets meaningfully more complex.

- **The continent/country maps were placeholder lists, not real maps** *(superseded — see the destination-screen entry below; `ContinentMapScreen`/`CountryMapScreen` no longer exist)*. The design doc originally called out evaluating a Flutter map/SVG package as separate follow-up work — building that now would have blocked the whole flow on a UI/rendering decision unrelated to data model or navigation. A real map (`countries_world_map` package) was in fact prototyped and wired in later on a dedicated branch, but that work was ultimately dropped in favor of a single search screen — kept for the record in case map-based destination picking gets revisited.

- **Destination picking collapsed from two screens (continent map → country map) into one search screen** — changed 2026-07-30 at Colleen's request: "Where are you going?" heading, a search field that filters the full country list directly (no continent grouping), and a decorative globe icon (`Icons.public`, no image asset added) filling the space below the search field when there's no query yet. This is a genuine product-direction change, not just an implementation swap — flagged the conflict with `language-app-system-design.md`'s documented two-screen map flow before building, and updated that doc's Requirements/User Flows/Architecture/Roadmap sections to match, per her call to keep the doc authoritative. New screen is `lib/screens/destination_screen.dart` (`DestinationScreen`), replacing the deleted `continent_map_screen.dart`/`country_map_screen.dart`. `TripSelection` lost `selectedContinent`/`selectContinent`/`countriesInContinent`/`isContinentActive` since nothing reads continent groupings anymore — `Country.continent` itself stays in the data model as informational/future-hook data (per the design doc). The map flow (real `countries_world_map` widget, pinch/zoom, tap-to-country-resolution, etc.) had actually been built and proven out on the `feature/map` branch earlier — that work isn't merged into `main` and isn't touched by this change, just no longer the plan going forward.

- **`countries.json` now lists all 219 real-world countries/territories, not just the 4 placeholders (Costa Rica, Mexico, Argentina, France)** — changed 2026-07-30 at Colleen's request ("all countries in the dropdown"). Sourced from the same 219-entry `id`/`name`/`continent` dataset built on the `feature/map` branch (`world_map_countries.json`, itself cross-referenced against a standard ISO region dataset — see that branch's history for how it was built), not hand-typed. Only `cr` is `active: true` — see the next entry for what `active` actually gates now. Two fields needed filling in that the source dataset didn't have: `languageCode` (a best-effort primary/official language per country, ISO 639-1 — **not independently verified**, low-stakes since no code path actually reads this field yet, but worth a real pass before it is) and `name` (a handful of ISO formal names swapped for common ones better suited to a search field — e.g. "Russian Federation" → "Russia", "Korea, Republic of" → "South Korea", "United States of America" → "United States" — full list of overrides is in this commit's diff).

- **Every country is selectable on the destination screen, but only `active` ones reach the real flow — the rest dead-end at a "coming soon" screen** — the tap-through-to-empty-flow version shipped 2026-07-30 ("for now let's have all countries be selectable... even if we don't have language info available yet") was itself superseded the same day once Colleen sent over a full rewrite of `language-app-system-design.md` calling for an explicit coming-soon screen instead ("Dead end by design — no further app flow for this country until content ships"). Flagged that reversal plus a second, bigger one in the same doc (content per-language vs. per-country) before touching anything; she confirmed keep-per-country and build-the-coming-soon-screen. `DestinationScreen._selectCountry` now branches on `country.active`: `true` proceeds to `CategorySelectionScreen` as before, `false` pushes the new `ComingSoonScreen` (`lib/screens/coming_soon_screen.dart`) instead. That screen reads a new `TripSelection.resources` (`List<Resource>`, loaded via `ContentRepository.loadResources()` from a new `assets/data/resources.json` — one shared set of generic links reused for every uncovered country, per the doc's explicit reasoning against curating per-country resources) and opens them with the new `url_launcher` dependency. `ContentRepository.loadContent`'s try/catch around the missing-asset case (added for the now-superseded empty-flow version) was kept rather than removed — `loadContent` is unreachable for inactive countries in the normal flow now, but it's a harmless safety net if `active` is ever flipped true ahead of its content file actually existing.
  - Regression test for this lives in its own file, `test/no_content_country_test.dart` (rewritten from testing the empty-flow behavior to testing the coming-soon screen), not appended to `widget_test.dart` — pumping a second full `AroundTheWordApp` in the same test file makes `pumpAndSettle` hang (confirmed with a two-line minimal repro, nothing specific to this scenario; a pre-existing `flutter_test` quirk, not a bug in the app). One full-app-boot test per file going forward avoids it.

- **One recursive `CategoryListScreen` instead of separate parent/child screens.** Use mode needs a category list, and (for categories with sub-subjects) a second screen showing just those sub-subjects. Rather than two widgets, `CategoryListScreen` takes an optional `subjectsToShow`/`title` and pushes itself again for a tapped subject's children when it has any, or a `VocabListScreen` when it doesn't. Same pattern the original English MVP used for the category→subject skip logic, just generalized to arbitrary depth instead of hardcoded to one level.

- **`TripSelection.personalize()` is written as a real `async` step**, even though today it's just a local map filter plus an artificial `Future.delayed(900ms)` for the cosmetic loading screen — per the doc's explicit instruction that this should be a one-line swap to a real backend call later, not a restructure.

- **Content files are looked up as `content/{countryCode.toLowerCase()}.json`** (e.g. `cr.json`) — lowercased so the convention doesn't depend on remembering to match `countries.json`'s casing exactly.

- **Flashcards use tap-to-flip + next/previous, no swipe gesture or flip animation.** The doc only specifies "tap or swipe to flip, swipe or button to advance" — went with the simpler half (tap, buttons) for the scaffold; swipe gestures and a real flip animation are additive, not a rework, if wanted later.

- **Deleted the old English-MVP `lib/` files** (`models/vocab_category.dart`, `data/vocab_data.dart`, the three old screens) rather than keeping them alongside the new structure — they're preserved in git history (commit `bdf23b2`), and keeping unused code around would just be confusing dead weight given the new doc fully supersedes that direction.

## Open Questions

- Real map rendering for the continent/country screens — which package, and how much of a scope item that is on its own, is still unevaluated.
- Placeholder content (`food-cooking`, `museums`, `hiking`) needs to become real, reviewed Costa Rica vocab — next step is running actual content through `tools/vocab_tagger/` rather than hand-writing tagged JSON.
- Flashcard visuals (flip animation, swipe gestures) are intentionally minimal right now — worth a pass once the core flow is validated, not before.

## Explicitly Not Done Yet

- No git commit of this scaffold.
- No real continent/country map rendering (see Open Questions).
- No accounts, persistence, or backend — matches current V1 scope exactly, not a gap.
- No exercise UI beyond flashcards (fill-in-blank, matching) — deferred per the roadmap, tags are captured but unused.

---

## Current State (of the original English MVP build — pre-pivot, historical)

A working Flutter prototype: launch the app → see a list of categories → tap one → see either a subject list (if the category has niche sub-subjects) or the phrase list directly (if it doesn't) → see the vocab words/phrases. All content is hardcoded in Dart, no backend, no persistence, nothing committed to git yet.

## Implementation Decisions (historical)

- **Three-level data model: `Category` → `Subject` → `Phrase`.**
  **Why:** the design doc's subject list already distinguishes broad groups (Food, Transportation) from niche sub-subjects within them (Cooking, Grocery Shopping) and explicitly flags this as "structure to keep in mind for how subjects are organized." Modeling it as two list levels (rather than a flat list with a category string) keeps that hierarchy real instead of implicit.

- **Categories with only one subject skip the subject-list screen and go straight to the phrase list.**
  **Why:** roughly half the v1 subject list (Hiking, Museums, Music, Occupations, etc.) has no sub-items. Forcing a tap through an intermediate screen with a single row would add friction the MVP's "pick a subject, see the words" framing doesn't call for. Categories that do have sub-items (Food, Shopping, Transportation, Celebrations) still show the intermediate list.

- **Added "20 Common Verbs" as a new category**, per direction to include one alongside the existing subject list. Structured the same way as any single-subject category (no special-casing in the model) — it's just a `Category` with one `Subject` holding 20 `Phrase`s.

- **Phrases are plain English text only — no translations, audio, or example sentences.** Matches "English only for the prototype" and keeps scope inside what the design doc's MVP section actually asked for (word/phrase lists, nothing more). The design doc's open question about what a subject should contain beyond a word list is still open — see below.

- **No new dependencies added** beyond the default Flutter scaffold (`cupertino_icons`, `flutter_lints`). Kept deliberately lean since this is a prototype and the data is static.

- **Rewrote the default counter widget test** to instead drive the real navigation flow (category list → subject list → phrase list, and the single-subject skip path) since the scaffold's test exercised the counter demo that no longer exists.

## Open Questions (historical, carried over + new)

- Still open from the design doc: what a subject contains beyond a word list (example sentences? audio?), and whether sub-subjects should be separately selectable or grouped. Now that real content exists, worth revisiting concretely rather than in the abstract.
- "20 Common Verbs" is structurally a category like any other, but conceptually different — it's a core-vocab list rather than a situational/topic list. Worth deciding whether that distinction should show up in the UI (e.g. a separate section) as more non-topic lists get added.
- Category and subject names in code are close to the design doc's wording but not identical in a few spots (e.g. "Hiking / The Outdoors") — worth a pass to make sure display names match what's wanted before this goes further.

## Explicitly Not Done Yet (historical)

- No git commit.
- No "learn vs. use" mode, personalization, or multi-language support (per MVP scope).
- No social/community layer (liking phrases, recommending places) — flagged in the design doc as post-MVP, data model wasn't specifically shaped around it yet.
- No persistence layer — content is a static Dart file, not a database or CMS.
